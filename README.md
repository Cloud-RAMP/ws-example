# WebSocket communication example in go

* First run `go mod tidy` to download dependencies
* Run `go run ./cmd/sever/server.go` to run the server
* Run `go run ./cmd/client/client.go` to run a demo client that sends and receives 2 messages
* To run the benchmark tests, open this project in vscode, examine the benchmark file, and hit the play button next to the desired test